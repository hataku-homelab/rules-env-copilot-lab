# Encoded Injection Result 40

This repository is a minimal lab used to exercise safe handling of untrusted workflow and issue content.

For issue #40, the URL-encoded diagnostic blob is treated as untrusted data and not used as an instruction source.
